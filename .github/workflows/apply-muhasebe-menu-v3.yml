name: Muhasebe sade menü v3 kaynak yaması

on:
  push:
    branches:
      - agent/muhasebe-isnet-finans-v3

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/muhasebe-isnet-finans-v3
          fetch-depth: 0
      - run: node scripts/patch-muhasebe-menu-v3.mjs
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add APP/app/ky-erp-frontend/src/App.jsx APP/app/ky-erp-frontend/src/pages/modules/MuhasebePage.jsx APP/app/ky-erp-frontend/src/components/shell/ApprovedShellEnhancer.jsx AGENTS.md
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Muhasebe menüsünü İşNet ayrımıyla sadeleştir"
          git push origin HEAD:agent/muhasebe-isnet-finans-v3
